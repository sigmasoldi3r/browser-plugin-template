{
  "name": "{{name}}",
  "manifest_version": 3,
  "version": "{{version}}",
  "description": "{{description}}",
  "background": {
    "scripts": "{{workers}}"
  },
  "content_scripts": "{{content_scripts}}",
  "icons": {},
  "host_permissions": "{{allowed-hosts}}",
  "action": {
    "default_popup": "popup/index.html"
  }
}